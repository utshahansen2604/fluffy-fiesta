pipeline {
    agent any
    stages {
        stage('ping') {
            steps {
                sh 'cd sbatchu1/ansible && ansible all -m ping'
            }
        }
        stage('playbook') {
            steps {
                sh 'cd sbatchu1/ansible && ansible-playbook *.yml -b'
            }
        }
    }
}
